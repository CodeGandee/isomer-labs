# fixture-method-team Execplan

This minimal Domain Agent Team Template fixture exercises generic template validation without copying `teams/deepsci-org/execplan/`. It contains only the files needed by the validator: manifest, participant contract, bindings, workspace/state/run contracts, instantiation schema, a generated skill path, one role profile path per role, and notifier prompts.
