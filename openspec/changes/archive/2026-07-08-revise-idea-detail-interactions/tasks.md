## 1. Graph Interaction

- [x] 1.1 Preserve graph node title, summary, one-liner, status, and record metadata in ReactFlow node data.
- [x] 1.2 Add delayed hover Markdown tooltip behavior for idea graph nodes without fetching full record detail.
- [x] 1.3 Change ReactFlow idea graph single click to select only and double click to open detail.
- [x] 1.4 Keep dense graph open behavior aligned with double-click where the renderer supports it.

## 2. Idea Detail Layout

- [x] 2.1 Remove the top-level idea detail `Copy JSON` action while keeping `View JSON`, `Copy Markdown`, and refresh.
- [x] 2.2 Move main record JSON, lineage, realizations, and diagnostics into a tabbed JSON dialog with `Main Record` selected by default.
- [x] 2.3 Add styling for compact hover Markdown and tabbed JSON viewing.

## 3. Validation

- [x] 3.1 Add or update frontend tests for single-click selection, double-click opening, hover preview, and JSON dialog tabs.
- [x] 3.2 Run focused frontend validation and fix regressions.
