## 1. Publication Models and Selection

- [x] 1.1 Add semantic publication content classes, explicit raw-byte settings, reference-repository bindings, and portable research-record index models.
- [x] 1.2 Extend file and semantic-root classification so default intent, environment, and durable records are selected, raw payload bytes require opt-in, and approved paper PDFs receive typed validation.
- [x] 1.3 Include selection settings, reference bindings, generated navigation, and reproduction limitations in projection manifests and plan fingerprints.

## 2. Sanitized Projection Outputs

- [x] 2.1 Implement contextual individual-identity sanitization that preserves normalized credential-free GitHub repository identities and blocks credentials.
- [x] 2.2 Implement deterministic publication README and portable research-record index rendering without mutating the Source Topic Workspace.
- [x] 2.3 Implement validated upstream reference-submodule rendering alongside same-remote topic-owned component metadata.

## 3. Operator Guidance

- [x] 3.1 Revise Topic Git publication planning, privacy, safety, initialization, synchronization, and output guidance for reproduction-first defaults and explicit raw-byte settings.
- [x] 3.2 Document the organization-provenance versus individual-identity boundary, latest-paper rule, access limitations, and neutral publication authorship.

## 4. Verification

- [x] 4.1 Add unit coverage for semantic defaults, raw-byte opt-ins, typed PDFs, identity sanitization, README and research-index output, reference repositories, manifests, and stale-plan fingerprints.
- [x] 4.2 Add or update integration and skill-contract coverage for full default publication topology and reproduction artifacts.
- [x] 4.3 Run OpenSpec validation, focused Topic Git tests, skill validation, Ruff, MyPy, and the default unit suite; resolve regressions.
