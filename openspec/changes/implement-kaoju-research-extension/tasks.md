## 1. Kaoju Family Foundation

- [x] 1.1 Create the `research-paradigm/kaoju/` package-asset root, family README, and the exact eleven `isomer-kaoju-*` skill directories with identity-matched `SKILL.md` and `agents/openai.yaml` files.
- [x] 1.2 Implement `isomer-kaoju-shared` with directly linked evidence, source-identity, survey-artifact, interaction/Gate, external-owner, lineage, and terminal-report contracts.
- [x] 1.3 Implement `isomer-kaoju-workspace-mgr` and `isomer-kaoju-frame` with Topic Workspace readiness, dataset-registry posture, resource boundaries, survey contracts, and owner-routing blockers.

## 2. Survey Stage Skills

- [x] 2.1 Implement `isomer-kaoju-discover` for five-source-class search, query provenance, work/version families, inclusion decisions, curated identity resolution, and bounded expansion routes.
- [x] 2.2 Implement `isomer-kaoju-acquire` for manifest-first dataset lookup, immutable material identity, Canonical External Repository handoff, access/license metadata, and governed acquisition blockers.
- [x] 2.3 Implement `isomer-kaoju-examine` for exact source locators, Source Digests and Source Access Blockers, paper-code mappings, Claim-Evidence Ledger updates, contradictions, and achieved verification depth.
- [x] 2.4 Implement `isomer-kaoju-reproduce` for intended-data reproduction and generated-data capability probes, preserving faithful, adapted, repaired, failed, and probe-specific evidence separately.
- [x] 2.5 Implement `isomer-kaoju-compare` for theory mode, empirical intent mode, and empirical results mode with domain-derived dimensions, mandatory Comparison Intent Documents, fairness checks, variability, and `not-comparable` outcomes.
- [x] 2.6 Implement `isomer-kaoju-audit` and `isomer-kaoju-synthesize` so audits diagnose without mutating evidence and synthesis writes only evidence-backed Survey Deltas, Field Summaries, Claim Status Tables, and Kaoju Dossiers.

## 3. Pipeline Commands

- [x] 3.1 Implement the `isomer-kaoju-pipeline` entrypoint with one-pass orchestration, shared callback handling, clarification-first control, accepted resume context, stage handoffs, and `complete`, `paused`, or `blocked` terminal reports.
- [x] 3.2 Add procedural command pages for `landscape-pass`, `curated-intake-pass`, and `direction-expansion-pass` with recipes and outputs matching UC-01 through UC-03.
- [x] 3.3 Add procedural command pages for `theory-comparison-pass`, `method-trial-pass`, and `comparative-pass` with recipes, intent Gates, and outputs matching UC-04 through UC-06.
- [x] 3.4 Add `audit-survey-pass` with mandatory audit-before-synthesis behavior matching UC-09.
- [x] 3.5 Add grouped `manage-survey` actions `list`, `show`, `status`, and `export`, and grouped `manage-dataset` actions `register`, `list`, `show`, `refresh`, and `remove`, including Topic Workspace owner routing for mutations.
- [x] 3.6 Verify the pipeline exposes no standalone source-audit, reproduction, refresh, resume, full-Kaoju, list-passes, or per-CRUD-verb subcommands.

## 4. Packaged Extension Integration

- [x] 4.1 Add the optional `kaoju` group and exact eleven-skill inventory to `manifest.toml`, with `kind = "extension"`, `extension_id = "kaoju"`, and `always_available = false`.
- [x] 4.2 Add deterministic `begin` and `end` callback insertion-point metadata for every Kaoju skill and ensure skill identities match their manifest paths.
- [x] 4.3 Update packaged system-skill, research-paradigm, namespace, and installation documentation to describe Kaoju, its optional installation selector, its use-case boundary, and its distinction from DeepSci.

## 5. Family-Aware Validation

- [x] 5.1 Refactor `scripts/validate_research_paradigm_skillset.py` around explicit family configuration while preserving all existing DeepSci inventory, placeholder, lineage, structured-output, and active-zone rules.
- [x] 5.2 Add Kaoju validation for exact inventory, `isomer-kaoju-*` namespace, frontmatter/manifest identity, near-top workflows, directly linked local resources, canonical Isomer terms, forbidden provider/local paths, approved pipeline commands, and absent generic procedures.
- [x] 5.3 Add or update deterministic validator diagnostics so failures identify the affected Kaoju file, line when available, and violated family rule.

## 6. Automated Tests

- [x] 6.1 Update system-skill asset tests for `core`, `deepsci`, and `kaoju` group discovery, path resolution, extension metadata, materialization, and callback filtering.
- [x] 6.2 Update installer and Project system-extension tests to select, install, list, remember, forget, upgrade, and uninstall the catalog-provided `kaoju` extension through existing generic behavior.
- [x] 6.3 Add Kaoju skill-contract tests for the eleven-skill identity inventory, procedural/helper/misc command separation, grouped CRUD actions, required shared references, and forbidden extra procedures.
- [x] 6.4 Add research-paradigm validator fixtures for a valid Kaoju family and failures involving missing skills, wrong namespace, manifest drift, broken references, stale domain terms, hard-coded providers or local paths, and command-surface drift.
- [x] 6.5 Run the complete existing DeepSci validator test suite and confirm the family-aware refactor preserves prior diagnostics and accepted material.

## 7. Verification and Handoff

- [x] 7.1 Run the research-paradigm validation script against the packaged asset root and fix every Kaoju or DeepSci diagnostic.
- [x] 7.2 Run targeted system-skill asset, installer, Project extension, Kaoju contract, and research-paradigm validator unit tests.
- [x] 7.3 Run `pixi run lint`, `pixi run typecheck`, and `pixi run test` from the repository root.
- [x] 7.4 Materialize or install the `kaoju` extension into a temporary target, verify all eleven flat skill directories and support files, and confirm DeepSci remains absent unless selected.
- [x] 7.5 Review active Kaoju guidance for provider neutrality, owner routing, evidence calibration, grouped CRUD operations, and absence of feature-design or OpenSpec runtime dependencies, then record final validation evidence in the change handoff.
