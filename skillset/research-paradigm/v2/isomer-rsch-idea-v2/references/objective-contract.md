# Objective Contract

Use this reference to keep ideation pointed at the real target. Placeholder definitions live in `../migrate/placeholders.md`.

## Workflow

When this reference is used, execute the following steps in order.

1. **State real target, trusted proxies, false-progress signals, constraints, metric, contribution frame, and exit rule**.

If the user's task does not map cleanly to these steps, use your native planning tool to build a step-by-step plan from this reference, the parent skill, and the available evidence, then execute the plan.

## Guidance

- State real target, trusted proxies, false-progress signals, constraints, metric, contribution frame, and exit rule.
