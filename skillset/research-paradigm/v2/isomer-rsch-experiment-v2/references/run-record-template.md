# Run Record Template

Use this reference to record the measured result. Placeholder definitions live in `../migrate/placeholders.md`.

## Workflow

When this reference is used, execute the following steps in order.

1. **Include contract, code or config delta, commands, logs, outputs, metric values, comparability verdict, claim update, baseline relation, failure mode, next route, and blocker if any**.

If the user's task does not map cleanly to these steps, use your native planning tool to build a step-by-step plan from this reference, the parent skill, and the available evidence, then execute the plan.

## Guidance

- Include contract, code or config delta, commands, logs, outputs, metric values, comparability verdict, claim update, baseline relation, failure mode, next route, and blocker if any.
