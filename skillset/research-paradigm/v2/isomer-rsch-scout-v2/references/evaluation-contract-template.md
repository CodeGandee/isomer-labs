# Evaluation Contract Template

Use `references/eval-contract-template.md` for the canonical preserved-path evaluation contract support page. Placeholder definitions live in `../migrate/placeholders.md`.

This compatibility page exists for older v2 references that used the expanded filename. The source-preserving runtime page is `references/eval-contract-template.md`.
