# Science Task Brief

Use this reference to frame a scientific computation or validation task before execution. Placeholder definitions live in `../migrate/placeholders.md`.

## Workflow

When this reference is used, execute the following steps in order.

1. **State domain, objective, claim type, inputs, outputs, package or solver needs, validation checks, resources, and downstream consumer**.

If the user's task does not map cleanly to these steps, use your native planning tool to build a step-by-step plan from this reference, the parent skill, and the available evidence, then execute the plan.

## Guidance

- State domain, objective, claim type, inputs, outputs, package or solver needs, validation checks, resources, and downstream consumer.
