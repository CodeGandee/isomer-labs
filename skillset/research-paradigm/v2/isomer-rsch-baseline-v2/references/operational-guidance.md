# Operational Guidance

Use this reference to keep comparator work bounded and evidence-first. Placeholder definitions live in `../migrate/placeholders.md`.

## Workflow

When this reference is used, execute the following steps in order.

1. **Use Workspace Runtime and Artifacts when available**.
2. **Use Execution Adapter Command Requests for command work**.
3. **Stop once the acceptance target is satisfied**.
4. **Escalate only when the lighter route cannot be trusted**.

If the user's task does not map cleanly to these steps, use your native planning tool to build a step-by-step plan from this reference, the parent skill, and the available evidence, then execute the plan.

## Guidance

- Use Workspace Runtime and Artifacts when available.
- Use Execution Adapter Command Requests for command work.
- Stop once the acceptance target is satisfied.
- Escalate only when the lighter route cannot be trusted.
