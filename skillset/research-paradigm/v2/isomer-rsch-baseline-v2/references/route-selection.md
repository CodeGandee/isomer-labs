# Route Selection

Use this reference to choose the cheapest route that can make comparator trust real. Placeholder definitions live in `../migrate/placeholders.md`.

## Workflow

When this reference is used, execute the following steps in order.

1. **Prefer attach, import, or verify-local-existing before reproduce**.
2. **Use repair only when a concrete failure point can be changed**.
3. **Use waive only when the downstream route has an explicit reason to proceed without a comparator**.
4. **Keep one dominant comparator route active**.

If the user's task does not map cleanly to these steps, use your native planning tool to build a step-by-step plan from this reference, the parent skill, and the available evidence, then execute the plan.

## Guidance

- Prefer attach, import, or verify-local-existing before reproduce.
- Use repair only when a concrete failure point can be changed.
- Use waive only when the downstream route has an explicit reason to proceed without a comparator.
- Keep one dominant comparator route active.
