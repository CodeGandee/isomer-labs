# Frontier Management

Use this reference to keep algorithm-first state explicit. Placeholder definitions live in `../migrate/placeholders.md`.

## Workflow

When this reference is used, execute the following steps in order.

1. **Separate candidate briefs, durable lines, implementation attempts, measured results, failures, and stopped routes**.
2. **Do not promote more than one route per pass unless the task is explicitly fusion**.

If the user's task does not map cleanly to these steps, use your native planning tool to build a step-by-step plan from this reference, the parent skill, and the available evidence, then execute the plan.

## Guidance

- Separate candidate briefs, durable lines, implementation attempts, measured results, failures, and stopped routes.
- Do not promote more than one route per pass unless the task is explicitly fusion.
