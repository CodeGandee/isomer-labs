# Plateau and Fusion

Use this reference to respond to repeated non-improvement or complementary lines. Placeholder definitions live in `../migrate/placeholders.md`.

## Workflow

When this reference is used, execute the following steps in order.

1. **Use plateau signals to trigger route review**.
2. **Fuse only when the mechanisms are complementary and the combined route has a testable claim**.

If the user's task does not map cleanly to these steps, use your native planning tool to build a step-by-step plan from this reference, the parent skill, and the available evidence, then execute the plan.

## Guidance

- Use plateau signals to trigger route review.
- Fuse only when the mechanisms are complementary and the combined route has a testable claim.
