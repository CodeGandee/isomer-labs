# Candidate Ranking

Use `references/candidate-ranking-template.md` for the canonical candidate-ranking support. Placeholder definitions live in `../migrate/placeholders.md`.

This compatibility page exists because the first v2 draft used a synthesized page name before the refactor migration restored source reference filenames.
