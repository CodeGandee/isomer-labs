# NCU Command Posture

When planning GPU analytical-modeling validation in a Pixi-managed project, prefer `pixi run ncu ...` over `ncu pixi run ...` unless current project evidence proves a different wrapper is required. Record the exact command, target device, kernel selector, metric list, and any profiler or harness failure that affects interpretation.
