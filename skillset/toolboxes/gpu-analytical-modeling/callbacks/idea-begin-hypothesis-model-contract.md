# Idea Begin Hypothesis Model Contract

Invoke `gpu-analytic-idea-prior` subcommand `hypothesis-model-contract`.

Purpose: before idea work hardens, require GPU model hypotheses to name source needs, hardware components, physical parameters, equations, assumptions, and evidence targets.

Treat this as supplemental prior guidance under the active DeepSci skill, project context, and user request.
