# Decision Begin Mismatch Route

Invoke `gpu-analytic-decision-prior` subcommand `mismatch-route`.

Purpose: before decision routing, classify GPU model/evidence mismatches and choose refine, add evidence, limit, park, block, or partial-support routes.

Treat this as supplemental prior guidance under the active DeepSci skill, project context, and user request.
