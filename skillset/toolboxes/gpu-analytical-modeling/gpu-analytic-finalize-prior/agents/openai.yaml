interface:
  display_name: "gpu-analytic-finalize-prior"
  short_description: "GPU analytical-modeling finalization prior for closure readiness and final evidence class"
  default_prompt: "Use $gpu-analytic-finalize-prior when routed by a Toolbox callback prompt or manually invoked for GPU analytical-modeling finalization guidance."
policy:
  allow_implicit_invocation: false
