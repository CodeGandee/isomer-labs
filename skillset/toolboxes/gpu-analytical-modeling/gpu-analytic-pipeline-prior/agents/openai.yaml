interface:
  display_name: "gpu-analytic-pipeline-prior"
  short_description: "GPU analytical-modeling pipeline prior for selecting the next evidence route"
  default_prompt: "Use $gpu-analytic-pipeline-prior when routed by a Toolbox callback prompt or manually invoked for GPU analytical-modeling pipeline guidance."
policy:
  allow_implicit_invocation: false
