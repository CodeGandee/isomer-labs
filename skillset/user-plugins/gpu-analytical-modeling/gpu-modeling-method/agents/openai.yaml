interface:
  display_name: "gpu-modeling-method"
  short_description: "Shape GPU analytical model sources, equations, and baselines"
  default_prompt: "Use $gpu-modeling-method to apply GPU source priority, analytical model shape, and baseline evidence-class discipline."
policy:
  allow_implicit_invocation: false
