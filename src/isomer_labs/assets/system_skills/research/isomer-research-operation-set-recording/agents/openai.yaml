metadata:
  version: "0.4.0"

interface:
  display_name: "isomer-research-operation-set-recording"
  short_description: "Accept worker outputs into durable research"
  default_prompt: "Use $isomer-research-operation-set-recording to inspect, accept, and verify this operation set without inventing research semantics."

policy:
  allow_implicit_invocation: true
