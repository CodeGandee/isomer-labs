# Core Welcome Migration Provenance

`org/src/` is the untouched snapshot of the former protected `isomer-op-entrypoint->welcome` bundle taken before the independent public rewrite. It includes the original entrypoint, agent metadata, and all ten linked reference pages. `org/analysis/analysis-of-isomer-op-welcome.md` records the inspected behavior, ownership boundaries, public routines, and evidence handoffs used by the rewrite.

No original runtime file was excluded from the audit copy. The active skill lives outside `org/`; validators and agents must treat this directory as passive provenance rather than executable guidance.
