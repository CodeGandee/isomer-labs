metadata:
  version: "0.6.0"

interface:
  display_name: "isomer-srv-houmao-interop"
  short_description: "Provide service-routed Houmao adapter support"
  default_prompt: "Use $isomer-op-entrypoint use houmao to perform this isomer-srv-houmao-interop task."
