metadata:
  version: "0.6.0"

interface:
  display_name: "isomer-srv-agent-env-setup"
  short_description: "Prepare Agent Workspace cwd readiness."
  default_prompt: "Use $isomer-op-entrypoint use agent-env to perform this isomer-srv-agent-env-setup task."
