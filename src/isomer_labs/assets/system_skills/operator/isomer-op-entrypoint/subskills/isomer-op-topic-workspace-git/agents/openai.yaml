metadata:
  version: "0.6.0"

interface:
  display_name: "isomer-op-topic-workspace-git"
  short_description: "Manage independent local Topic Workspace history and sanitized remote publication."
  default_prompt: "Use $isomer-op-entrypoint use topic-git to inspect or manage Topic Workspace Git."
policy:
  allow_implicit_invocation: false
