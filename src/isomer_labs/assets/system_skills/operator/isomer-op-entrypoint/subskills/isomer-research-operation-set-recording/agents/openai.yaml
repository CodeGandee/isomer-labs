metadata:
  version: "0.5.0"

interface:
  display_name: "isomer-research-operation-set-recording"
  short_description: "Accept worker outputs into durable research"
  default_prompt: "Use $isomer-op-entrypoint use operation-sets to perform this isomer-research-operation-set-recording task."

policy:
  allow_implicit_invocation: true
