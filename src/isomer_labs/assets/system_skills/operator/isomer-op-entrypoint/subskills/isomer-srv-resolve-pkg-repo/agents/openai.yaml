metadata:
  version: "0.5.0"

interface:
  display_name: "isomer-srv-resolve-pkg-repo"
  short_description: "Resolve reachable package repositories and channels"
  default_prompt: "Use $isomer-op-entrypoint use package-repo to perform this isomer-srv-resolve-pkg-repo task."
