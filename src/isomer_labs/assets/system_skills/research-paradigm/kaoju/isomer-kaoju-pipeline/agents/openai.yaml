interface:
  display_name: "isomer-kaoju-pipeline"
  short_description: "Route one bounded Kaoju survey procedure."
  default_prompt: "Use $isomer-kaoju-pipeline to select and run the bounded survey procedure that matches the request."
