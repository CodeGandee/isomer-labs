# Objective Contract

Use `references/objective-contract-template.md` for the canonical step support. Placeholder definitions live in `../migrate/placeholders.md`.

This compatibility page exists for older references to the previous production DeepSci migration. The operative guidance, preferences, constraints, and quality gates now live in `references/objective-contract-template.md`.
