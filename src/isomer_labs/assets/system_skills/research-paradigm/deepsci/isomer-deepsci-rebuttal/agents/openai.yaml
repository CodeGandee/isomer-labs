metadata:
  version: "0.2.3"

interface:
  display_name: "isomer-deepsci-rebuttal"
  short_description: "Turn reviewer feedback into response and revision work."
  default_prompt: "Use $isomer-deepsci-rebuttal to normalize reviewer feedback, route required fixes, and assemble a point-by-point response."
