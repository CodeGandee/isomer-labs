# Plateau and Fusion

Use `references/plateau-response-playbook.md` and `references/fusion-playbook.md` for the canonical plateau and fusion support. Placeholder definitions live in `../migrate/placeholders.md`.

This compatibility page exists because the first production DeepSci draft used a synthesized page name before the refactor migration restored source reference filenames.
