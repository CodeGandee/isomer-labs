schema_version = "isomer-research-topic-config.v1"
research_topic_id = "flash-attention-4-whitebox-runtime-model"
topic_statement = "Build a white-box, hardware-grounded model that, given a Flash Attention 4 forward-pass input configuration, predicts kernel runtime in milliseconds, the hardware component that will saturate, and the execution path that will block progress, for the NVIDIA B200 GPU across BF16/FP16/FP8/FP4 precisions and single-GPU configurations, without executing the kernel."
measurable_objectives = []
