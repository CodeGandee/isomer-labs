# Harness command registry — DeepResearch (general-purpose research loop).
#
# This is the loop-local deterministic command surface. Participant agents reach bookkeeping, gating,
# refinement, and rendering ONLY through these commands — never raw SQL or ad hoc state edits.
#
# Boundary (maintained Houmao skills own these; the harness must NOT):
#   - mailbox delivery / reply         -> houmao-agent-email-comms
#   - notifier enable/disable, gateway -> houmao-agent-gateway
#   - managed-agent lifecycle/launch   -> houmao-agent-instance / loop-pro execution stages
#   - memory pages                     -> houmao-memory-mgr
#   - workspace/worktree CREATION      -> houmao-utils-workspace-mgr
# The harness RECORDS intent + bookkeeping for the above and returns payloads to send; delivery is
# external. Git COMMIT/tag inside an already-created worktree is in-scope; worktree creation is not.
#
# `tier`:  "core" = domain-neutral, always present.   "extension" = domain-pluggable.
# `domain_pluggable=true` commands resolve a domain adapter from `knowledge_pack` (kind in
#   runner|validator|compiler|metric_vocab); with no enabled adapter they fall back to the generic
#   default noted per command.

[meta]
loop_slug        = "deepresearch"
registry_version = 1
state_backend    = "sqlite"
envelope         = "schemas/command-envelope.schema.json"
description      = "Deterministic harness surface: state, control, comms bookkeeping, findings, claims, self-wakeup, handoff, BO refiner, literature; plus domain-pluggable experiment/validate/render."

# Common output envelope for every command (machine-readable for agents).
[envelope]
fields = [
  "ok",            # boolean success
  "command",       # command identity, e.g. "state query"
  "quest_id",      # run id when known
  "plan_revision", # when known
  "data",          # command result payload
  "diagnostics",   # structured errors/notes
  "warnings",      # non-fatal advisories
]
explain_flag = "--explain"   # contract-exposing commands emit source-keyed descriptions
json_flag    = "--print-json"

[dependencies]
posture = "houmao-env"        # see dependency-posture.toml
click      = "CLI command groups"
jinja2     = ".md.j2 rendering (render/email)"
jsonschema = "record + email payload validation"

# ───────────────────────────────────────────────────────────────────────────
# CORE · framework control plane (domain-neutral)
# ───────────────────────────────────────────────────────────────────────────

[[command]]
name = "state init"
group = "state"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["../specs/state/schema.sql", "../specs/state/seed.toml"]
applies = ["runs/state.sqlite"]
summary = "Create the sqlite control plane from schema + seed for a quest."
description = "Initialize state.sqlite from ../specs/state/schema.sql and seed stage_catalog/knowledge_pack from seed.toml. Idempotent: re-init verifies an existing DB instead of overwriting."

[[command]]
name = "state validate"
group = "state"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["../specs/state/schema.sql", "../specs/state/invariants.toml", "runs/state.sqlite"]
applies = []
summary = "Check schema availability, referential integrity, allowed states, and named invariants."
description = "Run every check in invariants.toml (single_open_round, single_active_quest, baseline_gate_before_write, valid_backs_supported, wakeup_handoff_unique, handoff_attempts_bounded, gated_decision_confirmed, ...) plus FK/enum integrity and mail/artifact ref existence."

[[command]]
name = "state query"
group = "state"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite", "../specs/state/state-overview.md"]
applies = []
summary = "Read-only scheduler/recovery views (cursor, open wakeups, due handoffs, best result, claim readiness, frontier, intake)."
description = "Named views answering the state-overview scheduling queries. VIEW is a POSITIONAL argument (`state query <view>`), not a flag. Views: cursor, open-wakeups, due-handoffs, best-result, open-claims, branches, bo-observations, frontier (optimization frontier + incumbent), intake (audited assets by trust). Note: there is no `fanout` or `convergence` view — read fan-out completeness from the round (`received_handoffs` vs expected) / `handoff query`, and convergence posture from `bo status` + `findings query`. Output is structured enough for a skill to decide one bounded pass."

[[command]]
name = "state export"
group = "state"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/<quest-id>/exports/"]
summary = "Render a compact human-readable recovery/operator view."
description = "Emit a markdown snapshot of quest posture (cursor, branches, claims, decisions, open wakeups, due handoffs) for operator inspection via houmao-agent-inspect; no live UI."

# plan: DB-rendered living research map (pure projection; the DB stays canonical). Orchestrator-facing.

[[command]]
name = "plan render"
group = "plan"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite", "runs/<quest-id>/plan.md"]
summary = "Render runs/<quest-id>/plan.md from DB state and index it as artifact(kind='plan')."
description = "Pure projection of quest/branch/idea/frontier/decision/finalize/round/artifact rows into runs/<quest-id>/plan.md (Contract/Active-node/Route-frontier/Next-options/Disposition/Revisions sections), then records an artifact(kind='plan') (idempotent id <quest-id>:plan). The DB is canonical; plan.md is never read back as truth. Called at round close by deepresearch-orchestrator-tick. Requires --quest-id and --at; optional --out."

[[command]]
name = "plan status"
group = "plan"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Print the rendered plan map WITHOUT writing the artifact (inspection)."
description = "Read-only: emit the same projection as `plan render` plus the quest's current plan_revision, without writing plan.md or the artifact row."

[[command]]
name = "plan validate"
group = "plan"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite", "../specs/state/invariants.toml"]
applies = []
summary = "Read-only plan-map invariants (plan_map_fresh, plan_revision_monotonic) + decision-discipline lint + methodology-usage lint."
description = "Runs the plan invariants, a decision lint (warns on consequential decisions that chose among <2 named candidates or named no rejected alternative), and a methodology-usage lint (warns when a closed ORCHESTRATOR-INTERNAL round — decision/optimize/finalize — lacks an artifact(kind='methodology-usage'); worker stages are bound instead by their typed-record gates + `methodology check`, so they are not linted here; regime-gated to research-contract quests so history is exempt). Warnings are non-fatal; an invariant violation fails the command."

[[command]]
name = "plan diff"
group = "plan"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite", "runs/<quest-id>/objective/"]
applies = []
summary = "List recorded acceptance revisions (acceptance.md@rev-*) and confirmed amend-acceptance decisions."
description = "Read-only acceptance-amendment view: the current acceptance_ref, any append-only acceptance.md@rev-* files, and confirmed decision(route='amend-acceptance') rows. Acceptance amendments are operator-confirmed + append-only; the original contract is never overwritten."

[[command]]
name = "completeness audit"
group = "completeness"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Run the seven research-completeness checks (evidence/orphan/mechanism/alternatives/ablation/lit/discrepancy)."
description = "Advisory by default; the finalize gate enforces it ONLY in autonomy_mode='auto' at the gating rigor (DEEPRESEARCH_COMPLETENESS_GATE_RIGOR, default 'publication'). Required subset: evidence_traceability/no_orphan_claims/reference_lit_audit/discrepancy_handling always required; mechanism_explanation/named_alternatives/ablation_or_infeasibility required by default but waivable via DEEPRESEARCH_COMPLETENESS_REQUIRE_* env knobs. Run before review/finalize."

[[command]]
name = "record validate"
group = "record"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["../specs/state/records/*.schema.json", "../specs/state/schema.sql"]
applies = []
summary = "Validate a TOML/JSON record payload against its generated schema before apply."
description = "Schema-check a record (idea, experiment, result, measurement, analysis, claim, decision, finding, reference, branch, ...) so `record apply` stays narrow and transition-safe."

[[command]]
name = "record apply"
group = "record"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["../specs/state/records/*.schema.json", "runs/state.sqlite"]
applies = ["runs/state.sqlite"]
summary = "Apply a schema-valid record while preserving transition rules."
description = "The ONLY write path for agents. Enforces allowed status transitions and gated-decision rules. Idempotent by record id: re-applying the same id is a no-op, not a duplicate."

[[command]]
name = "control status"
group = "control"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite", "../agents/bindings.toml"]
applies = []
summary = "Report run_state, execution_mode, active participants, pending handoffs, blockers, next action."
description = "Read-only loop posture. Observed notifier posture is reported when available but enable/disable is routed through houmao-agent-gateway."

[[command]]
name = "control get-mode"
group = "control"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Return execution_mode + source evidence; default auto."
description = "Distinguish run_state from execution_mode. Defaults to auto when no explicit mode record exists."

[[command]]
name = "control set-mode"
group = "control"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite"]
summary = "Record an execution_mode change to auto|manual (operator_intent_event)."
description = "manual is NOT paused: it suspends notifier-driven wakeups so the operator prompts bounded turns. Records an operator_intent_event(kind='set-mode')."

[[command]]
name = "control pause"
group = "control"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite"]
summary = "Record pause intent and paused run posture."
description = "Set run_state='paused' and log operator_intent_event(kind='pause'). Blocks normal progress; distinct from manual mode."

[[command]]
name = "control resume"
group = "control"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite"]
summary = "Record resume intent and continuation posture."
description = "Set run_state='running' (or 'recovering' first when open wakeups/handoffs need reconciliation) and log operator_intent_event(kind='resume')."

[[command]]
name = "control stop"
group = "control"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite"]
summary = "Record stop intent and stop posture."
description = "Set run_state='stopped' with stop_reason and log operator_intent_event(kind='stop'). A low-quality stop requires a confirmed gated decision first."

[[command]]
name = "control manual-context"
group = "control"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite", "../agents/bindings.toml"]
applies = []
summary = "Return participant context for one manual-mode pass."
description = "Emit quest id, plan revision, run_state, execution_mode, participant id, pending mail/handoff refs, allowed actions, and stop_after_one_pass=true."

# Communication: bookkeeping + rendering only. Actual send/reply -> houmao-agent-email-comms.

[[command]]
name = "email schema"
group = "email"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["../specs/comms/templates.toml", "../specs/comms/schemas/*.schema.json"]
applies = []
summary = "Resolve a template name / schema_id to its schema + renderer paths."
description = "Maps the loop-local mail type (schema_id) to its JSON Schema and Markdown renderer for the templated families (task-request, task-result, receipt, self-wakeup, ...)."

[[command]]
name = "email validate"
group = "email"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["../specs/comms/schemas/*.schema.json"]
applies = []
summary = "Validate a TOML/JSON mail payload against its generated JSON Schema."
description = "Step 2 of TOML payload -> JSON Schema validation -> Markdown rendering -> Houmao delivery."

[[command]]
name = "email render"
group = "email"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["../specs/comms/templates.toml"]
applies = ["runs/<quest-id>/mail/"]
summary = "Render a validated payload through its Markdown (.md.j2) renderer."
description = "Produces a body containing a parseable in-body houmao-email-metadata block (carrying loop_id + handoff_id + schema_id). Delivery is performed externally via houmao-agent-email-comms."

[[command]]
name = "email apply"
group = "email"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite"]
summary = "Record mail lifecycle facts in mail_log (bookkeeping only, no delivery)."
description = "Insert/update a mail_log row (direction, schema_id, handoff_id, message_ref, status). Idempotent by payload_id. Never sends mail."

[[command]]
name = "email query"
group = "email"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite", "../specs/comms/templates.toml"]
applies = []
summary = "Expose payload refs, reply expectations, and schema_id dispatch facts."
description = "Lets on-event skills map an incoming schema_id to the owning handler and find reply expectations."

# ───────────────────────────────────────────────────────────────────────────
# CORE · research domain-neutral
# ───────────────────────────────────────────────────────────────────────────

[[command]]
name = "wakeup arm"
group = "wakeup"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite", "../specs/comms/templates.toml"]
applies = ["runs/state.sqlite"]
summary = "Insert a self_wakeup row and return the self-mail payload to send."
description = "Records the next-step intent (next_stage, reason, next_action, fresh handoff_id) as self_wakeup(status='armed') and renders the [self-wakeup] payload. The agent sends it via houmao-agent-email-comms, then calls `wakeup attach` with the message_ref. Idempotent by handoff_id; re-arming supersedes the prior row."

[[command]]
name = "wakeup attach"
group = "wakeup"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite"]
summary = "Record the opaque message_ref of a sent self-wakeup mail onto its row."
description = "Binds the delivered self-mail's message_ref to the armed self_wakeup row so recovery can correlate the open mail to its intent."

[[command]]
name = "wakeup resolve"
group = "wakeup"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite"]
summary = "Transition a self_wakeup row: delivered | consumed | superseded | failed."
description = "Called when the loop wakes on a self-mail (delivered), finishes the intended work (consumed), re-plans before firing (superseded), or fails. Late delivery of a superseded/consumed handoff_id is ignored (dedup)."

[[command]]
name = "wakeup list"
group = "wakeup"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "List open (armed|delivered) self-wakeups for recovery."
description = "The authority for 'what does the loop intend to do next'. Used by recover/resume to rebuild intent without a fragile live turn."

[[command]]
name = "gpu status"
group = "gpu"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Show the operator-confirmed GPU device set for a quest (confirmed?, devices)."
description = "Read the per-quest gpu_allocation. Used by the orchestrator-tick GPU gate before dispatching experiment/analysis handoffs and by the Experimenter/Analyst to learn which CUDA_VISIBLE_DEVICES they may use."

[[command]]
name = "gpu confirm"
group = "gpu"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["gpu.confirm"]
summary = "OPERATOR-ONLY: confirm the GPU device set a quest's GPU-using stages may use (e.g. --devices '0' or '0,1'; integer indices only)."
description = "Records gpu.confirm (status=confirmed) for the quest, satisfying the hard apply-time gate over experiment+analysis handoffs (invariant experiment_requires_gpu_confirmation) and enabling `experiment run`. devices must be a comma list of non-negative integers (schema-validated; 'all'/'-1'/empty rejected). Per-quest scope: confirm once; re-run only to change the device set. Agents must never self-confirm."

[[command]]
name = "handoff open"
group = "handoff"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite"]
summary = "Create/advance a handoff to 'sent' with due times; increment attempt_count."
description = "Stable (quest_id, handoff_id) ledger entry. Sets receipt_due_at/result_due_at and schema_id/roles. Reusing an existing handoff_id for a resend increments attempt_count rather than duplicating."

[[command]]
name = "handoff advance"
group = "handoff"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite"]
summary = "Transition a handoff: acked | result_received | processed | failed."
description = "Receivers mark progress here. Re-advancing an already 'processed' handoff_id is a dedup no-op. At attempt_count >= max_attempts the command refuses further sends and marks 'failed'."

[[command]]
name = "handoff query"
group = "handoff"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "List due/overdue/stalled or unobserved handoffs for resend decisions and dedup checks."
description = "Supports the relay/edge-loop supervisor: --due returns handoffs not in (processed,failed); --seen HANDOFF_ID answers the receiver dedup check; --stalled --now <iso> returns in-flight (sent|acked) handoffs past their receipt/result due time with attempts remaining (action=resend) or at max_attempts (action=fail+decision) — the liveness/heartbeat reconciliation input."

[[command]]
name = "findings add"
group = "findings"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite", "runs/<quest-id>/findings/"]
summary = "Persist a durable finding/reflexion lesson (always quest-owned; --quest-id required)."
description = "Write a finding_memory row grounded by a measurement/result ref. TOTAL QUEST ISOLATION: scope is always 'quest' and --quest-id is required; there is no cross-quest/global finding (invariant finding_quest_owned)."

[[command]]
name = "findings query"
group = "findings"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Recall THIS quest's findings/reflexion for idea refinement and convergence checks."
description = "Filter by kind/grounding. Quest-scoped: with no --quest-id it returns only the single running quest's findings (never cross-quest). Operator-only --all-quests gives a cross-quest view (agents must not use it)."

[[command]]
name = "claim upsert"
group = "claim"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite"]
summary = "Create/update a claim (statement + status). Convenience wrapper over `record apply --type claim.upsert`."
description = "Upserts a claim row. status open|supported|refuted|withdrawn (transition-guarded). A claim is 'supported' only with sufficient supports-evidence and no unresolved contradiction (see `evidence validate`)."

[[command]]
name = "claim resolve"
group = "claim"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite"]
summary = "Mark a contradicting evidence link resolved/rebutted. Wrapper over `record apply --type claim_evidence.resolve`."
description = "Sets resolved=1 (+ resolution_ref) on a claim_evidence row, so an acknowledged-and-rebutted contradiction no longer blocks 'supported'."

[[command]]
name = "claim link"
group = "claim"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite"]
summary = "Add/insert a claim, link evidence, or resolve a contradiction (result|analysis|measurement|reference|external)."
description = "Maintains claim + claim_evidence with relation supports|contradicts|contextualizes. A 'contradicts' link can later be marked resolved=1 (+ resolution_ref) to rebut it. Idempotent per (claim_id, source_kind, source_ref)."

[[command]]
name = "evidence validate"
group = "claim"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Audit the claim↔evidence map: coverage, orphan claims, unresolved contradictions."
description = "Domain-neutral synthesis gate. A claim may be 'supported' only with sufficient supports-evidence from valid results/analyses and no contradicts link with resolved=0 (rebutted contradictions are allowed). Used by write/review."

[[command]]
name = "outline validate"
group = "manuscript"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Structurally validate the typed paper-spine: one thesis, 1-3 falsifiable claims, not-claiming, mapped reviewer objections, section theses, display plan."
description = "structural pre-write gate. With a typed paper-spine (--spine-ref or the quest's paper_spine row) it validates exactly one thesis, 1-3 falsifiable main_claims (each with what_would_falsify_it + evidence_needed), a non-empty not_claiming boundary, reviewer_objections mapped to evidence/follow-up, a section thesis per experiment-section, and a display plan connected to claims; FAILS on any miss. The legacy markdown token scan (--artifact-ref) is advisory-only and does not gate finalize."

[[command]]
name = "manuscript validate"
group = "manuscript"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Validate manuscript-language hygiene: no route/operator/worktree/prompt wording in paper-facing prose."
description = "Domain-neutral manuscript hygiene gate (validate_manuscript_language). Flags loop-internal wording (route names, operator/worktree/handoff/lane/stage jargon, prompt state) that must not appear in the report/manuscript. Used by the Reviewer alongside `evidence validate`. (this language firewall is one sub-check of `manuscript coverage`.)"

[[command]]
name = "manuscript coverage"
group = "manuscript"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["paper_spine.submission_ready"]
summary = "Compute submission readiness from the typed paper-spine + claim/evidence/result rows; write submission_ready onto paper_spine."
description = "coverage validator. Computes paper_spine.submission_ready from: the typed spine validates; every main_claim resolves to a claim row with supporting evidence; no 'supported' claim lacks evidence; no result row is unmapped; not_claiming + limitations present; the manuscript states each main claim and carries no process/draft traces; display_plan present. Writes the validator-computed flag + coverage_json onto the paper_spine row (the Writer cannot self-certify). The finalize coverage gate reads this flag."

[[command]]
name = "review validate"
group = "review"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["review_verdict.valid", "review_verdict.route_target"]
summary = "Validate the latest typed review verdict (schema + actionability) and compute valid + route_target."
description = "review validator. Loads the latest review_verdict's typed verdict.json and FAILS unless: it matches the content schema; the row/artifact verdict agree; and it is ACTIONABLE (a borderline/reject verdict carries >=1 todo, and missing_experiments->experiment_todo, missing_analysis->analysis_todo, overclaims/unsupported_claims/rewrite_requirements->rewrite_todo). Writes the validator-computed valid flag + route_target onto the review_verdict row (the Reviewer cannot self-certify). The finalize review gate reads valid + verdict."

[[command]]
name = "review route"
group = "review"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Deterministic follow-up routing for the latest review verdict (experiment/analysis/write/finalize)."
description = "read-only routing the Orchestrator consumes after a review pass: reject/borderline route to experiment|analysis|write by the verdict's flaws (most-upstream first); accept routes to finalize. Returns the latest verdict, valid, operator_confirmed, route_target, and whether the review side permits finalization."

[[command]]
name = "review confirm"
group = "review"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["review_verdict.operator_confirmed"]
summary = "Operator: confirm the latest borderline verdict so it may finalize at standard rigor."
description = "operator action. Sets operator_confirmed=1 on the latest 'borderline' review verdict so the finalize review gate permits completion at standard rigor (publication never permits borderline). Errors if the latest verdict is not 'borderline'."

[[command]]
name = "idea validate"
group = "idea"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["idea_select.valid", "idea_select.gate_score"]
summary = "Validate the typed idea selection (slate/challenge/scored gate/rejections/retained) under rigor floors; compute valid + gate_score."
description = "idea-selection validator. Loads the latest idea_select's typed select.json and FAILS unless it passes the content schema AND the selection-pressure rules under the quest's rigor floor ([gate_floors]): raw_slate >= slate floor (rejects single-proposal/below-floor); strongest_rejection + outside_family_alternative present; >=1 rejected candidate with a concrete reason; retained in the raw slate; retained has mechanism + MVP experiment plan; selection scores sum consistently; retained total >= score floor; novelty_label != 'not_differentiated' (unless waived). Writes the validator-computed valid + gate_score onto the idea_select row (the worker cannot self-certify). The experiment-handoff idea gate reads valid."

[[command]]
name = "campaign validate"
group = "campaign"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["analysis_bridge.valid", "analysis_bridge.coverage_json"]
summary = "Validate the analysis bridge (paper-facing) AND per-claim campaign coverage by evidence_kind under rigor floors; compute valid."
description = "analysis->write readiness validator. FAILS unless (a) the latest analysis.bridge passes the typed paper-facing schema (claim->evidence map, paper-facing result paragraphs, limitations, figure/table recs) AND (b) every MAIN claim (kind='claim') meets the rigor coverage floor by evidence_kind ([gate_floors] campaign_required / campaign_baseline / campaign_anyof / campaign_significance_superiority) — claim-level, NOT a global experiment count. Writes the validator-computed valid + coverage_json onto the analysis_bridge row (the analyst cannot self-certify). The analysis->write handoff gate reads valid. An evidence_kind is counted ONLY when its result has validated provenance AND it carries valid kind-specific proof (e.g. ablation needs changed_factor/controls/delta + a resolvable parent_result; significance needs method+effect; reference/external needs an explicit source) — otherwise it is reported 'not counted' with a clear reason. Waive proof with DEEPRESEARCH_EVIDENCE_PROOF_GATE=0 (visible in gate status; finalize-sensitive)."

[[command]]
name = "scope validate"
group = "scope"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["scope_contract.valid"]
summary = "Validate the latest scope.contract (objective + evaluation plan is specific enough for the rigor); compute valid."
description = "Validator-owned scope/eval-contract gate. FAILS unless the latest scope.contract carries a concrete (non-vague) objective + a primary metric + metric direction (these are never waivable), plus research_question, non_goals, dataset, split, eval_protocol, false_progress_signals, baseline_route_expectation, acceptance_criteria — each either present or intentionally deferred via contract.waivers{field: reason}. Writes the validator-computed valid flag + a freshness fingerprint onto the scope_contract row (author cannot self-certify). In bound mode idea selection requires a valid, non-stale scope contract; `methodology check --stage scope` resolves to it. Waive with DEEPRESEARCH_SCOPE_GATE=0 (visible in gate status)."

[[command]]
name = "baseline validate"
group = "baseline"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["baseline_contract.valid"]
summary = "Validate the latest baseline.contract (route + eval contract + route-specific verification); compute valid."
description = "Validator-owned baseline-contract gate. FAILS unless the latest baseline.contract declares a baseline_route (reproduced|imported|trusted|waived), carries a primary metric + comparison policy + dataset/split/eval protocol (or an explicit waiver) + an expected-baseline reference, AND satisfies route-specific verification: reproduced -> evidence_ref resolves to a baseline result with validated provenance; imported/trusted -> evidence_ref cites a source/paper/leaderboard; waived -> a non-empty waiver_reason. Writes the validator-computed valid flag onto the baseline_contract row (the author cannot self-certify via verification_verdict). `_baseline_contract_gate` reads valid: baseline_gate=passed needs valid=1 on a real route; baseline_gate=waived needs valid=1 with baseline_route='waived'."

[[command]]
name = "gate status"
group = "gate"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Unified machine-readable status of every binding gate (idea/baseline/campaign/bridge/spine/outline/manuscript/review) + finalize readiness, with per-gate route_target."
description = "integration read. Returns structured JSON: per gate {status(pass|fail|advisory|not_applicable|missing|waived), rigor_level, blocking, reason, latest_record_ref, route_target, required_next_action} plus finalize_readiness and the blocking_gates list. Also surfaces active_waivers (env), durable_waivers (quality_gate.waiver rows), finalize_ack_present / finalize_ack_missing. READ-ONLY — it summarizes/guides Orchestrator routing; the hard guards (idea->experiment, baseline contract, analysis->write, finalize coverage/review, finalize waiver-ack) remain authoritative and are NOT replaced."

[[command]]
name = "gate waiver list"
group = "gate"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "List durable quality_gate.waiver records for a quest (audit view); record one via `record apply --type quality_gate.waiver`."
description = "Read-only audit view of the durable quality_gate.waiver rows (gate, source, reason, finalize_ack, actor, expiry, scope, created_at) for a quest. Durable waivers make env-var gate bypasses explicit and auditable: a BOUND quest cannot finalize 'complete' while a finalize-sensitive gate is env-waived unless a waiver with finalize_ack=true + a reason exists for that gate (the _finalize_waiver_ack_gate enforces this; gate status surfaces finalize_ack_missing). Record a waiver through the single write path: `record apply --type quality_gate.waiver` with gate/source/reason[/finalize_ack]."

[[command]]
name = "methodology check"
group = "methodology"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Resolve a task-result methodology_used[].applied_as to the stage's validated typed record (idea.select/baseline.contract/analysis.bridge/paper_spine/review.verdict)."
description = "methodology-evidence resolution. Given --quest-id --stage --applied-as, FAILS unless applied_as points at a real, same-quest typed record for that stage that passed its validator (idea_select.valid / baseline_contract acceptable verdict / analysis_bridge.valid / paper_spine.submission_ready / review_verdict.valid). Closes the 'methodology is claimable' hole: free-text methodology usage no longer counts as binding evidence (background-only reading belongs in methodology_consulted)."

[[command]]
name = "bo suggest"
group = "bo"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Propose the next point(s) over a declared search_space using observed primary measurements."
description = "Heuristic search-space refiner (NOT Bayesian optimization). Reads search_space + completed (experiment_param, is_primary measurement) pairs and returns next candidate point(s) honoring the declared objective sense. Skipped entirely when no search_space exists (heuristic refinement used instead). A real search policy (surrogate model / acquisition function) is provided only by installing a refiner adapter — the built-in is a placeholder. The caller records each suggestion as an idea/experiment with experiment_param.proposed_by='bo'. NOTE: the built-in policy is a PLACEHOLDER (returns a 'midpoint-default' suggestion with stub=true and a loud warning; it does NOT read observed results or update on negative findings) — install a bo-refiner adapter (knowledge_pack kind='refiner') for a real search policy. The stub must never be treated as optimization evidence."

[[command]]
name = "bo status"
group = "bo"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Report observed best, evaluated count, and a no-improvement streak (heuristic, NOT Bayesian optimization)."
description = "Quest-local optimization posture for plateau detection. Returns best_primary, observations, and an HONEST trailing no_improvement_streak computed from the ordered primary measurements (objective_sense=higher_is_better, assumed; see the scope contract's metric_direction). method='heuristic (not Bayesian optimization)' — no model/acquisition function (pairs with the placeholder `bo suggest`)."

[[command]]
name = "opportunity list"
group = "opportunity"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "List the quest-local research-opportunity ledger ('what to try next and why'); ADVISORY, never a gate."
description = "Read-only view of `research_opportunity` rows (opportunity_id, kind [next_experiment|ablation|robustness|boundary|baseline_repair|new_idea|failure_followup], status, rationale, motivating_refs, proposed_by, round_index, created_at) for a quest, open first then most-recent; optional --status filter. Each row carries an advisory `unresolved_refs` list (refs that are missing or owned by another quest). QUEST-LOCAL ONLY — motivating_refs cite this quest's finding/result/claim ids; there is no cross-quest/global discovery memory. Record an opportunity via the single write path: `record apply --type opportunity.record`. The discovery layer is advisory (it also appears in `gate status` data.discovery + `plan render`); it never blocks a transition or finalize."

[[command]]
name = "opportunity check"
group = "opportunity"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Resolve every opportunity's motivating_refs against THIS quest's rows (ADVISORY; never blocks)."
description = "Read-only check: for each `research_opportunity`, resolve `motivating_refs` quest-locally and report refs that are `missing` or owned by another quest (`cross_quest`). Supported ref types: finding/result/claim/claim_evidence/idea_select/experiment/analysis_bridge/paper_spine/branch/artifact/scope_contract/opportunity. ADVISORY ONLY — unresolved refs never enter blocking_gates and never block a transition or finalize; cross-quest refs are flagged invalid and never followed (no cross-quest memory). Returns all_resolved + a per-opportunity unresolved list."

[[command]]
name = "lit search"
group = "lit"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = []
applies = []
summary = "Search arxiv/web for sources matching a query (no state writes)."
description = "Generic external-knowledge search. Returns candidate sources (title, uri, source); the caller chooses what to fetch. Domain-neutral and available to every quest."

[[command]]
name = "lit fetch"
group = "lit"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite", "runs/<quest-id>/refs/"]
summary = "Fetch + cache a source and record a reference row."
description = "Caches content under runs/<quest-id>/refs/ and inserts a reference row (source, cite_key, title, uri, artifact_ref). Idempotent by uri; references are quest-owned (reference.quest_id NOT NULL, invariant reference_quest_owned) — no cross-quest cache reuse, each quest re-fetches its own sources. The caller links it to a claim via `claim link` (source_kind='reference')."

[[command]]
name = "lit query"
group = "lit"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "List/inspect recorded references and their evidence links."
description = "Read-only view over reference + claim_evidence for the Writer/Reviewer (e.g. building a references list)."

[[command]]
name = "lit bib"
group = "lit"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Emit a BibTeX .bib from the quest's recorded reference rows (for paper-latex --bib citations)."
description = "Writer step: turns recorded `reference` rows into a .bib the manuscript cites against; `render report` (paper-latex) resolves \\cite via --bibliography. Pairs with `lit search`/`lit fetch` to build a real Related Work + bibliography."

[[command]]
name = "lit audit"
group = "lit"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Scholarship-bar audit: enough quest-owned reference rows + >=1 claim positioned against a reference."
description = "Upgrade-1 scholarship check for one quest (--quest-id): counts this quest's `reference` rows and reference-backed claims against the min thresholds, plus soft warnings (all-`manual` bib, missing Related Work). The same check backs the hard finalize gate; run before review/finalize. See execplan/docs/publication-quality.md."

[[command]]
name = "selfcheck"
group = "meta"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["../specs/state/records/", "../specs/state/invariants.toml"]
applies = []
summary = "Static harness self-test: deps, record-type schemas <-> RECORD_MAP, and invariant count."
description = "No DB needed. Verifies python deps (click/jinja2/jsonschema/tomllib), that every record schema maps to a RECORD_MAP entry and vice versa (reports schemas/mapped counts), and loads the invariant set (count). Used by the start-runbook pre-flight."

[[command]]
name = "manuscript bundle"
group = "manuscript"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Emit a submission bundle from durable state (evidence ledger + claim-evidence map + submission checklist)."
description = "Finalize/write step: from claims, claim_evidence, analyses, references, and artifacts, writes evidence_ledger.md, claim_evidence_map.json, and an auto-derived submission_checklist.md (figures present? compiled PDF present? every supported claim has evidence?). Read-only over the DB; writes files only — the publication-grade audit bundle."

# Git: commit/tag/branch-record inside an EXISTING worktree. Worktree creation -> workspace-mgr.

[[command]]
name = "git checkpoint"
group = "git"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite", "../specs/workspace/workspace.toml"]
applies = ["runs/state.sqlite"]
summary = "Commit (and optionally tag) durable artifacts in the active worktree; record the artifact row."
description = "Auto-checkpoint of durable outputs into the operator-owned git workspace, then index them in the artifact table. Push is off by default. Does NOT create worktrees."

[[command]]
name = "git branch-record"
group = "git"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite", "../specs/workspace/workspace.toml"]
applies = ["runs/state.sqlite"]
summary = "Record a branch/worktree ref + status in the branch table (preserve failed routes)."
description = "Registers a git branch and its (workspace-mgr-created) worktree_ref in the branch table and updates status active|parked|abandoned|merged. Materializing the worktree itself is delegated to houmao-utils-workspace-mgr."

[[command]]
name = "git status"
group = "git"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["../specs/workspace/workspace.toml"]
applies = []
summary = "Report working-tree status for the active worktree."
description = "Read-only git posture to confirm a checkpoint is clean before stage transitions."

# ───────────────────────────────────────────────────────────────────────────
# EXTENSION · domain-pluggable (adapter resolved from knowledge_pack; generic default otherwise)
# ───────────────────────────────────────────────────────────────────────────

[[command]]
name = "experiment run"
group = "experiment"
tier = "extension"
domain_pluggable = true
adapter_kind = "runner"
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite", "../specs/workspace/workspace.toml"]
applies = ["runs/state.sqlite", "runs/<quest-id>/round-<n>/"]
summary = "Execute an experiment's run contract under the operator-confirmed GPU set (fail-closed); capture results/measurements honestly."
description = "FAIL-CLOSED GPU enforcement: refuses to run unless the quest has a confirmed gpu_allocation, and injects CUDA_VISIBLE_DEVICES=<confirmed devices> into the child process so executed code (kernels, benchmarks, ncu, generated code) is physically restricted to the confirmed set. With --cmd '<command>' [--cwd <dir>] [--timeout <s>] it runs that command under the injected env and returns rc/stdout/stderr; with no --cmd it reports the confirmed device set (stub until a knowledge_pack(kind='runner') adapter overrides it). Never invents metrics."

[[command]]
name = "result validate"
group = "experiment"
tier = "extension"
domain_pluggable = true
adapter_kind = "validator"
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite"]
summary = "Apply the domain validity gate to a result (sets result.validity) and compute its provenance_ok."
description = "Default adapter checks metric completeness vs the objective contract and comparability vs baseline, setting validity valid|invalid|incomparable. Domains override via knowledge_pack(kind='validator') for correctness gates (e.g. allclose, statistical significance). This is the hard gate before a result can back a supported claim or update best_result_ref. ALSO computes the validator-owned provenance_ok flag from the result's declared provenance_route + manifest (executed needs command/code_revision-or-worktree/metric-source/run_status, failure needs failure_mode; imported/trusted need a source/note; waived needs a waiver_reason) — campaign coverage will not count this result's evidence_kind unless provenance_ok=1, or DEEPRESEARCH_PROVENANCE_GATE=0 waives enforcement. ALSO computes the validator-owned provenance_level (declared | artifact_backed [a log/config/output reference resolves to an artifact row + metric_source] | external_trusted [imported/trusted] | waived; reexecuted is reserved). At PUBLICATION rigor a local result must be >= artifact_backed to be counted, unless DEEPRESEARCH_ARTIFACT_PROVENANCE_GATE=0 waives it."

[[command]]
name = "metric validate"
group = "experiment"
tier = "extension"
domain_pluggable = true
adapter_kind = "metric_vocab"
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Validate measurement metric_name/unit against an enabled metric vocabulary."
description = "Optional. When a knowledge_pack(kind='metric_vocab') is enabled, checks that recorded measurement names/units are known for the domain; otherwise accepts any name (fully general)."

[[command]]
name = "render report"
group = "render"
tier = "extension"
domain_pluggable = true
adapter_kind = "compiler"
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite", "../specs/comms/templates.toml"]
applies = ["runs/state.sqlite", "runs/<quest-id>/report/"]
summary = "Render a report/manuscript from supported claims + evidence; index the artifact."
description = "Default adapter emits Markdown from supported claims, results, and references. Domains override via knowledge_pack(kind='compiler') for LaTeX/PDF or other formats. Records an artifact(kind='report'|'bundle')."

[[command]]
name = "render figure"
group = "render"
tier = "extension"
domain_pluggable = true
adapter_kind = "compiler"
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite", "runs/<quest-id>/figures/"]
summary = "Render a figure/plot from measurements; index the artifact."
description = "Default adapter produces simple plots from measurement rows. Domains override via knowledge_pack(kind='compiler'). Records an artifact(kind='figure')."

[[command]]
name = "render plot"
group = "render"
tier = "extension"
domain_pluggable = true
adapter_kind = "compiler"
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite", "runs/<quest-id>/figures/"]
summary = "Publication-quality plot from CSV/JSON data via the enabled compiler adapter (paper-plot)."
description = "Loads + calls the enabled compiler pack's adapter (paper-plot is a real stdlib SVG plotter; nature-figure for that domain). Options: --input <csv|json>, --ref <out figure path>, --title. Writes the figure to --ref and records an artifact(kind='figure'). With no enabled compiler pack it falls back to the generic stub (records the artifact, renders nothing). See packs/ADAPTER-CONTRACT.md."

[[command]]
name = "render polish"
group = "render"
tier = "extension"
domain_pluggable = true
adapter_kind = "compiler"
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite", "runs/<quest-id>/figures/"]
summary = "Render-inspect-revise QA pass on an existing figure (figure-polish)."
description = "Resolves the enabled compiler pack (`figure-polish`/`nature-figure`); default stub records a polished artifact + QA notes. For paper_main/appendix surface classes."

[[command]]
name = "render slides"
group = "render"
tier = "extension"
domain_pluggable = true
adapter_kind = "compiler"
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite", "runs/<quest-id>/report/slides/"]
summary = "Build a presentation deck from a paper/report (nature-paper2ppt)."
description = "Resolves the enabled compiler pack (`nature-paper2ppt`); default stub records an artifact(kind='bundle', a .pptx). Selects story figures + speaker notes."

[[command]]
name = "manuscript polish"
group = "manuscript"
tier = "extension"
domain_pluggable = true
adapter_kind = "template"
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite", "runs/<quest-id>/report/"]
summary = "Polish/restructure manuscript prose to a venue style (nature-polishing)."
description = "Loads the enabled template pack adapter (`nature-polishing`); options --input <text/md>, --ref <out>. Writes a polished artifact + editor notes; style only — never adds unsupported claims. Generic stub (artifact recorded) when none enabled."

[[command]]
name = "manuscript datastmt"
group = "manuscript"
tier = "extension"
domain_pluggable = true
adapter_kind = "template"
bin = "bin/deepresearch"
mutates_state = true
idempotent = true
reads = ["runs/state.sqlite"]
applies = ["runs/state.sqlite", "runs/<quest-id>/report/"]
summary = "Generate a Data Availability statement + FAIR/dataset-citation package (nature-data)."
description = "Resolves the enabled template pack (`nature-data`); default stub records an artifact(kind='report', data-availability). Drafts only from verified data inventory; never invents DOIs/accessions."

[[command]]
name = "knowledge query"
group = "knowledge"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "List enabled knowledge packs (adapters/references) by domain/kind; discover e.g. the science scipkg catalog."
description = "Read-only discovery over the knowledge_pack registry. Agents use it to find enabled domain knowledge (science-scipkg cards, mentor standards, compiler/template/runner/validator adapters) and their refs."

[[command]]
name = "knowledge cards"
group = "knowledge"
tier = "core"
domain_pluggable = false
bin = "bin/deepresearch"
mutates_state = false
idempotent = true
reads = ["runs/state.sqlite"]
applies = []
summary = "Aggregate package/standard cards from enabled reference packs (science-scipkg, mentor-standards)."
description = "Read-only. Loads each enabled reference pack's adapter `cards()` (or its `catalog.json`) and returns the entries, optionally filtered by --domain/--query. Used by the Experimenter (science package cards) and the mentor skill (standards)."
