# action: nature (nature-figure → Nature/high-impact figure)
`$HARNESS --via skill:deepresearch-figure/nature:<role> render figure --quest-id <q> --artifact-id <q>:fig-<name> \
  --ref runs/<q>/figures/<name>.pdf --input <data>`
Fill the figure-contract and run the qa-contract checklist (nature-figure/references/*). Nature domain; render to `.pdf`.
