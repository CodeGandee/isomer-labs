{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "deepresearch/records/_envelope",
  "title": "record-apply envelope",
  "description": "Common wrapper for every record-apply payload. Authority for the fields family schemas embed.",
  "type": "object",
  "required": ["record_type", "record_id", "at"],
  "properties": {
    "record_type": { "type": "string", "pattern": "^[a-z_]+\\.[a-z_]+$", "description": "<family>.<op> discriminator" },
    "record_id": { "type": "string", "description": "idempotency key = affected row PK (composite keys joined with ':')" },
    "at": { "type": "string", "description": "caller-supplied ISO-8601 timestamp" }
  }
}
