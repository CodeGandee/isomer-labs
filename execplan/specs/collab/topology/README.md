# Topology

## Purpose

Machine-readable topology contract for the DeepResearch tree-loop, consumed by harness topology
validation/lookup and by generated skills/agent bindings.

## Contents

- `topology.toml`: mode (`tree-loop`), root (`orchestrator`), specialist routes (shared task-request → receipt → task-result families), the self-wakeup continuation edge, and harness tool edges.
