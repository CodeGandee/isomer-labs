# Pack: intake-rubric

- kind: reference (methodology; surfaced via `knowledge cards` / `knowledge query`)
- origin: ported from DeepScientist `intake-audit` skill (unprefixed dir).
- status: **methodology references** — the Scout/Ideator READS these when a quest starts from pre-existing assets.

> Purpose: audit/trust-rank/reconcile pre-existing assets at quest entry before choosing the next anchor — four intake questions, trust + manuscript-visibility ranking, state buckets, adoption rules, current-board packet.

> Houmao adaptation note: record outcomes via `intake_asset.record` / `decision.record` (DB-canonical). The 5-value DeepScientist trust vocabulary maps onto Houmao's 4-value `intake_asset.trust` enum (see intake-method.md). These references are advisory craft, never an authoritative state surface.

## Index (references/)
- `intake-method.md`
- `state-audit-template.md`
