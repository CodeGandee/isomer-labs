# Profiles

## Purpose

Per-agent prompt material referenced by `../bindings.toml`.

## Contents

- `<agent-id>/definition.md`: the role system prompt (identity, owned stages, contract usage, reply protocol).
- `<agent-id>/memo-seed.md`: the birth-time memo seed (short, stable role identity).
