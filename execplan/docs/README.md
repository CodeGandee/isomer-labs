# Docs

## Purpose

Generated human support views for the DeepResearch loop. Orientation only — not source authority.
The full support set (operator guide, runtime model, validation notes, consistency/omission notes) is
generated by `execplan-finalize`; until then `../manifest.toml` is the authoritative artifact index and
`../specs/collab/collab-overview.md` is the process authority.

## Contents

- `start-runbook.md`: exact ordered, tagged (read-only / state-mutating / launch-starting) commands to take the package live, with rollback/teardown. Not executed until operator-approved.
- (pending `execplan-finalize`) operator-guide.md, runtime-model.md, artifact-index.md, validation.md, consistency-notes.md, omission-notes.md.
