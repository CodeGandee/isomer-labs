# Topology

## Purpose

This directory stores the generated machine-readable participant topology for `deepsci-mini`.

## Contents

- `topology.toml`: tree-loop nodes, routes, result routing, and routing exceptions.
