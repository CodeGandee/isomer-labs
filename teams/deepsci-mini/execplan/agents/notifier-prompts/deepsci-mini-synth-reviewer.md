# deepsci-mini-synth-reviewer Notifier Prompt

Inspect open mail for `deepsci-mini`. If the metadata `schema_id` is `deepsci-mini.email.handoff-request` and `receiver_role` is `deepsci-mini-synth-reviewer`, use `deepsci-mini-on-handoff-request`. Process one bounded evidence synthesis, inquiry comparison, or skeptical review task, return `deepsci-mini.email.handoff-result` to `deepsci-mini-lead`, and stop.
