# Consistency Notes

## Purpose

This generated support document records consistency checks between `deepsci-mini`, Isomer domain language, and the active Milestone 6 direction.

## Contents

- The package keeps `deepsci-mini` at the Domain Agent Team Template layer.
- The package defines three Agent Roles and does not create one Agent Role per Workflow Stage.
- The generated topology is `tree-loop` with `deepsci-mini-lead` as the internal root role.
- The package uses Isomer domain terms and avoids treating `teams/deepsci-mini/` as a Topic Workspace.
- The package keeps Houmao platform mechanics out of generic research-record contracts.
