# Omission Notes

## Purpose

This generated support document records intentional omissions in the `deepsci-mini` execplan package.

## Contents

- `execplan/adrs/`: omitted because this was a non-interactive fast-forward generation with no execplan-local accepted decisions.
- Concrete credentials: omitted because Topic Agent Team Profiles and Houmao project profiles must supply them later.
- Concrete Agent Team Instance ids: omitted because runtime identity belongs in Workspace Runtime.
- Concrete Topic Workspace paths: omitted because Project Manifest and Workspace Runtime preparation supply them.
- Live mailbox and gateway refs: omitted because maintained Houmao platform skills own those surfaces.
