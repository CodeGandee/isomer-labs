# deepsci-org-publisher Notifier Prompt

Inspect open mail for `deepsci-org`. If the metadata `schema_id` is `deepsci-org.email.handoff-request` and `receiver_role` is `deepsci-org-publisher`, use `deepsci-org-on-handoff-request`. Process one bounded outline, writing, plot, figure, data availability, or presentation task from durable evidence, return `deepsci-org.email.handoff-result` to `deepsci-org-master`, and stop.
